version: 1
frontend:
  phases:
    preBuild:
      commands:
        - chmod +x build.sh
    build:
      commands:
        - ./build.sh
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/* 